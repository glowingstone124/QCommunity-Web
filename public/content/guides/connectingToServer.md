# 如何连接到服务器？

Quantum Original 当前游戏版本为 `1.21.8`。连接服务器不需要正版账号，也不需要安装 Mod。

服务器采用聚合链路，多个节点均可通过 `qoriginal.vip` 访问。若首次连接失败，请稍后重试。

进入服务器后，可以使用以下指令切换分服：

- `/server creative`：切换到创造服
- `/server survival`：切换到主服

如果多次尝试后仍无法连接，请在群内联系管理员。服务器源站可能正在维护。
